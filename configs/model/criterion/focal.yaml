# @package model.criterion
# Too many false positives: decrease alpha
# Too many false negatives / masks too small: increase alpha

_target_: src.models.components.loss.BinaryFocalLoss
_partial_: true
alpha: 0.25
gamma: 2.0
