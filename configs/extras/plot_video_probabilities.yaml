defaults:
  - default.yaml

# log plot to wandb
after_test_plots:
  _target_: src.utils.plots.PlotVideosProbabilities
  enabled: true
  data_dir: null
  video_dataset_dir: null
  batch_size: null
  input_size: [55, 80]
  min_probabilities: []
  probability_norm: 1.0
