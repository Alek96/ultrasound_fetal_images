# @package _global_

defaults:
  - head_segmentation.yaml
  - override /model/criterion@model.criterion: dice_focal

model:
  model:
    _target_: segmentation_models_pytorch.FPN
    encoder_name: tu-tf_efficientnetv2_s
  criterion:
    alpha: 0.25
    gamma: 2.0
    smooth: 1e-6
