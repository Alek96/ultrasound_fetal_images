# @package _global_

defaults:
  - head_segmentation.yaml

tags: ["test-17"]

model:
  model:
    _target_: segmentation_models_pytorch.MAnet
    encoder_name: tu-tf_efficientnetv2_s

logger:
  wandb:
    group: "manet"
