# @package _global_

defaults:
  - head_segmentation.yaml

tags: ["test-06", "arch_sweep"]

model:
  model:
    _target_: segmentation_models_pytorch.MAnet
    encoder_name: tu-mobilenetv4_conv_small

logger:
  wandb:
    group: "manet"
